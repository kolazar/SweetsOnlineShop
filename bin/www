#!/usr/bin/env node

/**
 * Module dependencies.
 */

var app = require('../app');


var port = process.env.PORT || 3000;

/**
 * Listen on provided port, on all network interfaces.
 */

app.listen(port, function(){
  console.log('App is running on koallashop.herokuapp.com:' + port);
});

